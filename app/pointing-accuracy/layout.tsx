import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Pointing Accuracy',
};

const PointingAccuracyLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default PointingAccuracyLayout;
