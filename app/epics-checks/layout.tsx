import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Epics Checks',
};

const EpicsChecksLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default EpicsChecksLayout;
