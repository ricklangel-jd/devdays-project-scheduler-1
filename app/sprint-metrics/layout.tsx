import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sprint Metrics',
};

const SprintMetricsLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default SprintMetricsLayout;
