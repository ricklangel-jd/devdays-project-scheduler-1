import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Effort Estimates',
};

const EffortEstimatesLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default EffortEstimatesLayout;
