import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Time Spent',
};

const TimeSpentLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default TimeSpentLayout;
