import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sprint Planning',
};

const SprintPlanningLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default SprintPlanningLayout;
