import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Capacity v Demand',
};

const CapacityVDemandLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default CapacityVDemandLayout;
