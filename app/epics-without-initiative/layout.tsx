import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Epics Without Initiative',
};

const EpicsWithoutInitiativeLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default EpicsWithoutInitiativeLayout;
