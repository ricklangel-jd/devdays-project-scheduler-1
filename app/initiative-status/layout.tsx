import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Initiative Status',
};

const InitiativeStatusLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default InitiativeStatusLayout;
