import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Estimates Needed',
};

const EstimatesNeededLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default EstimatesNeededLayout;
