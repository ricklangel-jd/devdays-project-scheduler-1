import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Capacity',
};

const CapacityLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default CapacityLayout;
