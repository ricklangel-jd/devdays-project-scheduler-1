import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'All Work',
};

const AllWorkLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default AllWorkLayout;
