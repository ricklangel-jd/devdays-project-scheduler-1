import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'PI Planning',
};

const PiPlanningLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default PiPlanningLayout;
